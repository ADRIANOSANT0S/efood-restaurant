class Dish {
  id: number
  img: string
  title: string
  description: string

  constructor(id: number, img: string, title: string, description: string) {
    this.id = id
    this.img = img
    this.title = title
    this.description = description
  }
}

export default Dish
